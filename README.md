১)  Type বুঝতে সহজ, Interface টা একটু জটিল। type declaration এর সময় property এর পর সমান চিহ্ন দিতে হয়, যে interface এ লাগে না। এক interface কে extend  করে অন্য interface এ ব্যবহার করা যায়, যা  type এ করা যায় না।


২) keyof এর কাজ হলো declare করা type of object থেকে   key গুলোকে বের করে string হিসেবে union করে ধারণ করা।